# Login page for Material react table practice app

Use with `https://github.com/YasuhiroOsajima/go-auth-api`.

```bash
npm run build
cp -a out/* /work_dir/go-auth-api/webserver/nginx_document/login/
```
